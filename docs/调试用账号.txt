═══════════════════════════════════════
  实习通 · 调试用账号
  （完整项目后端 + 数据库模式）
═══════════════════════════════════════

使用前提：MySQL 已启动 + 已导入种子数据 + 后端已启动（node app.js）

───────────────────────────────────────
🛡️ 管理员（全部权限，无需绑定邮箱）
───────────────────────────────────────
  用户名：admin
  密  码：admin123
  说  明：可登录管理后台 /admin/login.html

───────────────────────────────────────
✨ 会员用户（已绑定邮箱，可发布/收藏/评论）
───────────────────────────────────────
  用户名：张三
  密  码：123456

───────────────────────────────────────
📋 普通用户（只能浏览，发布需先绑定邮箱）
───────────────────────────────────────
  用户名：王同学    密  码：123456
  用户名：陈学长    密  码：123456
  用户名：刘学姐    密  码：123456

───────────────────────────────────────
数据库导入方式
───────────────────────────────────────
  mysql -u root -p internship_platform < schema-mysql.sql
  mysql -u root -p internship_platform < seed-data.sql
  mysql -u root -p internship_platform < alter-migration.sql

启动方式
───────────────────────────────────────
  前端：双击 start-server.bat → http://localhost:8080
  后端：cd server && node app.js → http://localhost:3000
